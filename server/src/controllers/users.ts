module.exports = {
    userInfo: async (req:any, res:any) => {
        res.json('userInfo')
    },

    userPatch: async (req:any, res:any) => {
        res.json('userInfo')
    },

    userDelete: async (req:any, res:any) => {
        res.json('userInfo')
    }
}